#!/bin/sh

if [ "$1" = install ] || [ "$1" = update ]; then
    if [ -n "${CYDIA:-}" ]; then
        cydia_fd=${CYDIA%% *}
        eval "echo 'finish:restart' >&$cydia_fd"
    else
        echo "Please respring your device."
    fi
fi

exit 0